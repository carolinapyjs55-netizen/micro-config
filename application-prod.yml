spring
  applicaation:
    name: Config-Produc
eureka:
  instance:
    prefer-ip-adress: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
